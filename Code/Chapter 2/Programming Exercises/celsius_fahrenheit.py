# Ask user the temperature in Celcius
celcius = float(input('Temperature in Celcius: '))

# Calculation to Fahrenheit
fahrenheit = (9/5) * celcius + 32

# Display Results
print('Temperature in Fahrenheit:', format(fahrenheit, '.1f'))
